ADD_LIBRARY (gtest-lib STATIC
    gtest-all.cc
)

SET(LIB_NAME gtest)
IF(MSVC)
    #SET(LIB_NAME "lib${LIB_NAME}_vc140")
ENDIF()
IF(${CMAKE_BUILD_TYPE} STREQUAL Debug)
    #SET(LIB_NAME "${LIB_NAME}_d")
ENDIF()
SET_TARGET_PROPERTIES(gtest-lib PROPERTIES OUTPUT_NAME "${LIB_NAME}")
