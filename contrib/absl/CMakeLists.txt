ADD_LIBRARY (absl-lib STATIC
    numeric/int128.cc
)

SET(LIB_NAME absl)
IF(MSVC)
    SET(LIB_NAME "lib${LIB_NAME}_vc140")
ENDIF()
IF(${CMAKE_BUILD_TYPE} STREQUAL Debug)
    SET(LIB_NAME "${LIB_NAME}_d")
ENDIF()
SET_TARGET_PROPERTIES(absl-lib PROPERTIES OUTPUT_NAME "${LIB_NAME}")
